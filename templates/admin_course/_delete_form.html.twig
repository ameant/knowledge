<form method="post" action="{{ path('app_admin_course_delete', {'id': course.id}) }}" onsubmit="return confirm('Supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ course.id) }}">
    <button class="alert-btn">Supprimer</button>
</form>
